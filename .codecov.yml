codecov:
  notify:
    require_ci_to_pass: yes

comment:
  layout: "diff, files"

coverage:
  precision: 2
  round: down
  range: 90..100
  status:
    project:
      default:
        target: 100%
    patch:
      default:
        target: 100%
    changes: no
